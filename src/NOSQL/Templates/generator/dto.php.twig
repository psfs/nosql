<?php
namespace {{ domain }}\Dto\Models;

use \NOSQL\Dto\Model\NOSQLModelDto;

final class {{ model }} extends NOSQLModelDto {
{% for property in properties %}
    /**
     * @var {{ property.type }}
{% if property.required %}
     * @required
{% endif %}
{% if property.type == 'enum' %}
     * @values {{  property.enum }}
{% endif %}
     * @label {{ property.description|default(property.name) }}{% if property.type == 'enum' %} ({{ property.enum }}){% endif %}

     */
    public ${{ property.name }};

{% endfor %}
}